import { q } from './helpers.js';

const rstpPerVlanStpBank = [
  q('d2-2-9-q1', 'What is the main benefit of RSTP compared with classic STP?', ['It removes the need for a root bridge', 'It converges faster after topology changes', 'It disables blocked ports permanently', 'It turns every link into Layer 3'], 1, 'easy', 'RSTP keeps the same loop-prevention goal as STP but improves convergence speed when the topology changes.'),
  q('d2-2-9-q2', 'What is an edge port in RSTP?', ['A port expected to connect to an end device and transition quickly to forwarding', 'A port that always becomes the root port', 'A trunk that carries only the native VLAN', 'A port reserved for VTP advertisements'], 0, 'medium', 'Edge ports are designed for end devices, not switch-to-switch links, and can move to forwarding more quickly.'),
  q('d2-2-9-q3', 'Which RSTP role provides an immediate standby path toward the root bridge?', ['Alternate port', 'Designated port', 'Native port', 'Voice port'], 0, 'medium', 'An alternate port is the ready backup path toward the root in RSTP logic.'),
  q('d2-2-9-q4', 'What does Rapid PVST+ provide?', ['One STP instance for every campus regardless of VLAN count', 'Rapid convergence behavior for per-VLAN spanning-tree operation', 'A wireless roaming protocol', 'A replacement for EtherChannel'], 1, 'medium', 'Rapid PVST+ keeps spanning-tree operation per VLAN while using rapid-convergence behavior.'),
  q('d2-2-9-q5', 'What is the main scalability goal of MST?', ['To disable STP on trunk links', 'To replace VLANs with IP subnets', 'To group multiple VLANs into shared spanning-tree instances', 'To advertise routes between controllers'], 2, 'medium', 'MST improves scale by allowing many VLANs to share a smaller number of spanning-tree instances.'),
  q('d2-2-9-q6', 'Which statement about RSTP is correct?', ['RSTP changes the goal from loop prevention to load balancing', 'RSTP still prevents Layer 2 loops while reacting faster to failures', 'RSTP works only on access ports', 'RSTP blocks all redundant links permanently'], 1, 'easy', 'RSTP is still a loop-prevention protocol; its key difference is faster convergence behavior.'),
  q('d2-2-9-q7', 'Which set of logic helps RSTP react faster to topology events?', ['Alternate, backup, and edge-port logic', 'Only native VLAN tagging', 'VTP revision control', 'CDP timer tuning'], 0, 'medium', 'RSTP accelerates recovery by identifying faster-transition roles such as alternate and edge ports.'),
  q('d2-2-9-q8', 'What is an operational benefit of per-VLAN spanning-tree behavior?', ['Every VLAN is forced to use the same exact root bridge', 'Different VLANs can have different spanning-tree instances and root decisions', 'Trunk links stop carrying multiple VLANs', 'MAC learning is disabled'], 1, 'medium', 'Per-VLAN spanning tree lets different VLANs follow different logical topologies and root placements if needed.'),
  q('d2-2-9-q9', 'Where should an edge port normally connect?', ['To another switch carrying several VLANs', 'To a wireless controller uplink only', 'To an end host or similar non-switching device', 'To the root bridge only'], 2, 'medium', 'An edge port is intended for endpoints, not for switch-to-switch links where loops are possible.'),
  q('d2-2-9-q10', 'Why is MST considered a scalability design rather than a different loop-prevention purpose?', ['Because it allows more VLANs to share fewer STP instances', 'Because it eliminates bridge priorities', 'Because it converts trunks into routed ports', 'Because it requires CAPWAP'], 0, 'medium', 'MST keeps the same loop-prevention function but scales better by grouping VLANs into shared instances.'),
];

export default rstpPerVlanStpBank;
