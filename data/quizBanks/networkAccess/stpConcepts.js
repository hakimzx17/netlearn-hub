import { q } from './helpers.js';

const stpConceptsBank = [
  q('d2-2-8-q1', 'What problem does Spanning Tree Protocol solve on a switched LAN?', ['It prevents Layer 2 loops on redundant topologies', 'It assigns IP addresses to access ports', 'It learns neighbor routes dynamically', 'It encrypts wireless client traffic'], 0, 'easy', 'STP prevents bridging loops by intentionally keeping some redundant Layer 2 paths from forwarding traffic.'),
  q('d2-2-8-q2', 'How is the root bridge elected in classic STP?', ['Lowest bridge ID wins, based on priority and then MAC address', 'Highest MAC address always wins', 'The switch with the most ports becomes root', 'The first switch powered on wins'], 0, 'easy', 'The root bridge is the switch with the lowest bridge ID, which is built from bridge priority and then MAC address as the tiebreaker.'),
  q('d2-2-8-q3', 'On a non-root switch, what is the root port?', ['The port with the lowest path cost toward the root bridge', 'The port connected to the newest neighbor', 'The port with the highest MAC address', 'The port that carries the native VLAN'], 0, 'medium', 'Each non-root switch chooses one root port as its best path back to the root bridge.'),
  q('d2-2-8-q4', 'What is a designated port?', ['A port used only for VTP advertisements', 'A port that always blocks', 'A port chosen per switch instead of per segment', 'The forwarding port selected for a given LAN segment'], 3, 'medium', 'On each shared segment, STP chooses one designated port to forward traffic for that segment.'),
  q('d2-2-8-q5', 'Why does STP block some redundant paths?', ['To keep the topology loop-free while still preserving redundancy', 'To increase collisions on half-duplex links', 'To prevent VLAN tagging', 'To disable MAC learning'], 0, 'easy', 'The blocked state is the mechanism STP uses to stop loops while keeping backup paths available if the active path fails.'),
  q('d2-2-8-q6', 'What is a likely result of redundant Layer 2 links with no loop-prevention mechanism?', ['Broadcast storms and MAC-table instability', 'Automatic inter-VLAN routing', 'Wireless roaming failure only', 'Native VLAN encryption'], 0, 'medium', 'Without loop prevention, switched LANs can spiral into broadcast storms and unstable MAC learning behavior.'),
  q('d2-2-8-q7', 'Which statement about the root bridge is correct?', ['It blocks all ports by default', 'Its active ports on connected segments are normally designated ports', 'It cannot forward broadcasts', 'It learns only destination MAC addresses'], 1, 'medium', 'Because the root bridge is the reference point, its ports on active segments are normally designated forwarding ports.'),
  q('d2-2-8-q8', 'When STP compares multiple paths toward the root, which path is preferred?', ['The path with the lower accumulated cost', 'The path with the higher MAC address', 'The path with the more VLANs', 'The path with the newer switch'], 0, 'medium', 'Lower path cost is preferred when selecting the best route back to the root bridge.'),
  q('d2-2-8-q9', 'Which port roles normally forward traffic in an STP topology?', ['Root ports and designated ports', 'Blocked ports and backup ports', 'Only ports on the root bridge', 'Only trunk ports'], 0, 'medium', 'Root ports and designated ports are the normal forwarding roles in a stable STP topology.'),
  q('d2-2-8-q10', 'What is the most reliable way to solve an STP role question?', ['Start by changing the priority and hoping', 'Evaluate only the native VLAN', 'Work through root election, path cost, and segment-by-segment role decisions', 'Assume the shortest cable always wins'], 2, 'hard', 'STP questions become predictable when you solve them in order: root bridge, best path to root, then designated-versus-blocked decisions per segment.'),
];

export default stpConceptsBank;
