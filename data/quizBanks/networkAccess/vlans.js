import { q } from './helpers.js';

const vlansBank = [
  q('d2-2-4-q1', 'At the CCNA level, what is a VLAN?', ['A Layer 2 broadcast domain', 'A Layer 3 routing protocol', 'A WAN encapsulation format', 'A Layer 4 security tunnel'], 0, 'easy', 'A VLAN is a logical Layer 2 broadcast domain created on shared switching infrastructure.'),
  q('d2-2-4-q2', 'What kind of traffic pattern is normal on a standard access port connected to a host?', ['Multiple tagged VLANs on one port', 'One access VLAN for that attached host', 'Only routed traffic with no Layer 2 behavior', 'Only voice-tagged traffic'], 1, 'easy', 'Access ports normally carry one VLAN for the attached endpoint.'),
  q('d2-2-4-q3', 'What is required for traffic to move from one VLAN to another?', ['A native VLAN mismatch', 'A Layer 3 routing function', 'A MAC table age timer', 'A VTP transparent switch'], 1, 'easy', 'VLANs are separate Layer 2 domains, so inter-VLAN communication requires routing.'),
  q('d2-2-4-q4', 'Which command is commonly used to verify VLAN existence and port membership on a switch?', ['show ip route', 'show interfaces trunk', 'show vlan brief', 'show cdp neighbors'], 2, 'easy', 'show vlan brief is a standard operational check for existing VLANs and access-port assignments.'),
  q('d2-2-4-q5', 'Two PCs are connected to the same switch and placed in the same VLAN. What is normally true?', ['They can communicate at Layer 2 without a router between them', 'They must traverse the default gateway for every frame', 'They are automatically isolated by STP', 'They need CAPWAP to exchange traffic'], 0, 'easy', 'Hosts in the same VLAN share the same local Layer 2 broadcast domain and can switch traffic without routing.'),
  q('d2-2-4-q6', 'If a host is assigned to the wrong VLAN, what is most directly affected?', ['The switch boot process', 'Whether the host belongs to the expected local broadcast domain', 'The interface duplex setting', 'The clock rate on the link'], 1, 'medium', 'Wrong VLAN membership places the host in the wrong Layer 2 segment, which breaks expected local reachability.'),
  q('d2-2-4-q7', 'Why is VLAN segmentation useful in enterprise switching?', ['It eliminates the need for IP addressing', 'It isolates broadcasts and improves control on shared infrastructure', 'It forces every port to become a trunk', 'It makes STP unnecessary'], 1, 'medium', 'VLANs shrink broadcast scope and give engineers more control over who shares the same Layer 2 segment.'),
  q('d2-2-4-q8', 'What does a VLAN ID identify?', ['A logical segment on the shared switch infrastructure', 'A physical copper pair', 'A routing process ID', 'A wireless controller MAC address'], 0, 'medium', 'The VLAN ID marks the logical Layer 2 segment that traffic belongs to inside the switching environment.'),
  q('d2-2-4-q9', 'What is normally true about routers and VLAN broadcast domains?', ['Routers extend the same Layer 2 broadcast domain across VLANs', 'Routers separate VLANs instead of forwarding their Layer 2 broadcasts by default', 'Routers learn MAC addresses for every switch port', 'Routers tag the native VLAN on access ports'], 1, 'medium', 'Routing creates a Layer 3 boundary, so broadcasts do not simply pass between VLANs like one large switch domain.'),
  q('d2-2-4-q10', 'Which verification focus best matches VLAN troubleshooting?', ['Check only the cable color and duplex', 'Confirm VLAN existence, correct port membership, and realistic reachability expectations', 'Assume every active port is in VLAN 1', 'Ignore operational output and rely only on design diagrams'], 1, 'medium', 'VLAN verification is about confirming the VLAN exists, the right ports belong to it, and the expected reachability makes sense.'),
];

export default vlansBank;
