import { q } from './helpers.js';

const cliBasicsBank = [
  q('d2-2-2-q1', 'Which command moves you from user EXEC mode into privileged EXEC mode?', ['configure terminal', 'enable', 'copy running-config startup-config', 'reload'], 1, 'easy', 'The enable command elevates you from the limited user EXEC prompt to privileged EXEC mode.'),
  q('d2-2-2-q2', 'What does the command "configure terminal" do?', ['Saves the running configuration', 'Erases startup configuration', 'Enters global configuration mode', 'Returns to user EXEC mode'], 2, 'easy', 'configure terminal moves the session into global configuration mode so device settings can be changed.'),
  q('d2-2-2-q3', 'Which command makes the current running configuration survive a reload?', ['show running-config', 'copy running-config startup-config', 'erase startup-config', 'debug all'], 1, 'easy', 'Changes persist across reloads only after the running configuration is copied into startup-config.'),
  q('d2-2-2-q4', 'The prompt "Switch(config-if)#" tells you that you are currently in which context?', ['User EXEC mode', 'Privileged EXEC mode', 'Global configuration mode', 'Interface configuration mode'], 3, 'easy', 'The (config-if) prompt means commands now apply to a specific interface configuration context.'),
  q('d2-2-2-q5', 'Which symbol provides context-sensitive help in Cisco IOS?', ['!', '?', '#', '*'], 1, 'easy', 'IOS uses the question mark to display available commands or parameters at the current prompt.'),
  q('d2-2-2-q6', 'Which command returns you directly to privileged EXEC mode from a nested configuration context?', ['enable', 'disable', 'end', 'logout'], 2, 'medium', 'The end command exits nested configuration modes and returns to privileged EXEC.'),
  q('d2-2-2-q7', 'How do you normally remove an existing configuration line in IOS?', ['Prefix the command with no', 'Use delete before the command', 'Use clear terminal', 'Use disable from configuration mode'], 0, 'medium', 'Most IOS configuration lines are removed by entering the same command with the no keyword in front of it.'),
  q('d2-2-2-q8', 'Which workflow reflects safe day-one IOS operation?', ['Change first, troubleshoot later, save if needed', 'Show the current state, make the change, verify the result, then save', 'Erase startup-config before every change', 'Reload after every command to confirm it works'], 1, 'medium', 'A clean operator workflow is to inspect the current state, make the change in the right mode, verify it, and then save if the result is correct.'),
  q('d2-2-2-q9', 'What is the main difference between user EXEC and privileged EXEC mode?', ['User EXEC is used only for routing protocols', 'Privileged EXEC is more limited than user EXEC', 'User EXEC provides limited monitoring while privileged EXEC provides broader operational access', 'There is no practical difference between them'], 2, 'medium', 'User EXEC is intentionally limited, while privileged EXEC is the main operational mode for show commands and access to configuration entry.'),
  q('d2-2-2-q10', 'Why is prompt awareness important in IOS?', ['It tells you how much memory the switch has left', 'It tells you which commands are valid and how much authority you currently have', 'It disables syntax checking', 'It replaces the need for show commands'], 1, 'easy', 'The prompt is the fastest indicator of both your current mode and the command scope available from that context.'),
];

export default cliBasicsBank;
