import { q } from './helpers.js';

const vlanTrunking8021qBank = [
  q('d2-2-5-q1', 'What is the main purpose of a trunk link?', ['To carry a single user VLAN to one host', 'To carry multiple VLANs across one physical link', 'To disable native VLAN forwarding', 'To replace the MAC address table'], 1, 'easy', 'Trunk links let multiple VLANs traverse a single physical connection while preserving VLAN identity.'),
  q('d2-2-5-q2', 'What does 802.1Q provide on a trunk?', ['A routing protocol for VLANs', 'Tagging that preserves VLAN identity across the link', 'An STP root election method', 'A replacement for duplex negotiation'], 1, 'easy', '802.1Q adds VLAN tagging so traffic crossing a trunk still identifies which VLAN it belongs to.'),
  q('d2-2-5-q3', 'By default, how is native VLAN traffic sent on an 802.1Q trunk?', ['Encrypted', 'Tagged twice', 'Untagged', 'Dropped'], 2, 'easy', 'The native VLAN is untagged by default on standard 802.1Q trunk behavior.'),
  q('d2-2-5-q4', 'Why does a native VLAN mismatch matter operationally?', ['It can create unexpected traffic handling across the trunk', 'It disables all MAC learning on the switch', 'It forces DTP to use access mode', 'It removes the need for STP'], 0, 'medium', 'If the two sides disagree on which VLAN is native, untagged traffic can be interpreted incorrectly.'),
  q('d2-2-5-q5', 'Which statement about DTP outcomes is correct?', ['Only one side matters when deciding whether a trunk forms', 'DTP outcomes depend on the combination of both interface modes', 'DTP works only on routed ports', 'DTP always creates a trunk on every active link'], 1, 'medium', 'DTP behavior is determined by what both sides are configured to do, not by one interface in isolation.'),
  q('d2-2-5-q6', 'What is the usual outcome when one side is dynamic desirable and the other side is dynamic auto?', ['The link stays down', 'The link forms a trunk', 'The link becomes a routed port', 'The switches disable DTP'], 1, 'medium', 'dynamic desirable actively tries to form a trunk, and dynamic auto will accept that negotiation.'),
  q('d2-2-5-q7', 'What is the usual outcome when both sides are left at dynamic auto?', ['A trunk still forms immediately', 'The link becomes err-disabled', 'One side becomes the root bridge', 'A trunk does not form by negotiation alone'], 3, 'medium', 'Two dynamic auto interfaces both wait, so they do not proactively form a trunk together.'),
  q('d2-2-5-q8', 'Which link is a common trunk use case in campus switching?', ['A switch-to-switch infrastructure link', 'A single host connected to an access port', 'A console cable', 'A loopback interface'], 0, 'easy', 'Trunks are commonly used between switching devices and other infrastructure links that need multiple VLANs.'),
  q('d2-2-5-q9', 'If one side is hard-coded as an access port and the other side is hard-coded as a trunk, what should you expect?', ['A mode mismatch with incorrect forwarding expectations', 'Perfect native VLAN operation', 'Automatic Layer 3 routing', 'A VTP transparent domain'], 0, 'medium', 'If the two sides disagree on whether the link is access or trunk, traffic handling will not match the design intent.'),
  q('d2-2-5-q10', 'Which traffic is normally untagged on an 802.1Q trunk?', ['The management VLAN only', 'All voice VLAN traffic', 'The native VLAN traffic', 'Every VLAN if STP is enabled'], 2, 'easy', 'Under normal 802.1Q operation, the native VLAN is the one sent without a tag by default.'),
];

export default vlanTrunking8021qBank;
