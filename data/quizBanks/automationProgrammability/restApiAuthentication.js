import { q } from './helpers.js';

const restApiAuthenticationBank = [
  q('d6-6-7-q1', 'Why does API authentication matter?', ['APIs are control surfaces and should be protected like other management interfaces', 'APIs cannot change network state', 'Authentication only matters for GUIs', 'APIs replace authorization'], 0, 'easy', 'API authentication matters because APIs can expose sensitive control and data.'),
  q('d6-6-7-q2', 'What best describes basic authentication at a high level?', ['Credentials are presented directly with the request', 'It is always delegated through OAuth', 'It is a routing protocol', 'It removes the need for TLS'], 0, 'easy', 'Basic authentication presents credentials directly with the request.'),
  q('d6-6-7-q3', 'What is an API key best understood as?', ['A value identifying an approved calling application or context', 'A DHCP binding', 'A QoS marking', 'A certificate authority'], 0, 'medium', 'An API key identifies an approved application or request context.'),
  q('d6-6-7-q4', 'What does a bearer token represent in an API design?', ['A presented token that grants current access', 'A stronger form of DNS', 'A VLAN trunk identifier', 'A physical interface role'], 0, 'medium', 'Bearer tokens act as access artifacts that grant current access when presented.'),
  q('d6-6-7-q5', 'Why must bearer tokens be protected carefully?', ['Possession of the token may be enough to obtain access', 'They are only used for logging', 'They disable OAuth', 'They are the same as syslog facilities'], 0, 'medium', 'Bearer tokens must be protected because possession may be enough to prove access.'),
  q('d6-6-7-q6', 'What is the main value of OAuth 2.0 in API security discussions?', ['Delegated access without directly exposing the user password to every application', 'It removes the need for identity', 'It is a replacement for JSON', 'It only works with switches'], 0, 'medium', 'OAuth 2.0 is valuable because it supports delegated access models.'),
  q('d6-6-7-q7', 'Which statement best matches API-key usage?', ['Useful for straightforward service integration where a managed key is distributed', 'The same as a northbound API', 'Always more secure than all token designs', 'Only for wireless guest access'], 0, 'medium', 'API keys are common in straightforward service integrations where a key can identify the caller.'),
  q('d6-6-7-q8', 'Which comparison is most accurate?', ['Basic auth is simple; OAuth 2.0 is more aligned with delegated authorization scenarios', 'OAuth 2.0 is simpler than basic auth in all cases', 'API keys replace every other method', 'Bearer tokens are not part of authentication discussions'], 0, 'hard', 'Basic auth is simpler, while OAuth 2.0 is more aligned with delegated and mature authorization scenarios.'),
  q('d6-6-7-q9', 'What is the best first decision when choosing an API authentication method?', ['Match the method to the trust, delegation, and integration requirements', 'Always use the simplest possible method regardless of risk', 'Ignore what the API can do', 'Choose based on VLAN count'], 0, 'medium', 'Method selection should follow the trust model and integration requirements.'),
  q('d6-6-7-q10', 'What is the best summary of REST API authentication?', ['Different methods exist because different integration and security models need different proofs of identity and access', 'Only one API auth method is valid', 'Authentication is separate from security', 'All API auth works the same way'], 0, 'hard', 'Different API authentication methods exist because integration and security needs differ.'),
];

export default restApiAuthenticationBank;
