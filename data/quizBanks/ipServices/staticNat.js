import { q } from './helpers.js';

const staticNatBank = [
  q('d4-4-2-q1', 'What best describes static NAT?', ['A many-to-one translation using ports', 'A permanent one-to-one address mapping', 'A dynamic route to the Internet', 'An NTP synchronization process'], 1, 'easy', 'Static NAT creates a permanent one-to-one mapping between an inside local and an inside global address.'),
  q('d4-4-2-q2', 'Why is static NAT commonly used for servers?', ['Because it provides a stable public identity for inbound reachability', 'Because it removes the need for DNS', 'Because it creates multiple translations from one address', 'Because it automatically enables DHCP relay'], 0, 'easy', 'Static NAT is useful when an inside server needs a stable public address for outside access.'),
  q('d4-4-2-q3', 'Which statement about static NAT and public address use is correct?', ['One public IP can represent unlimited internal servers through static NAT alone', 'A classic static NAT mapping usually consumes one public address per published inside host', 'Static NAT never uses public addressing', 'Static NAT works only with link-local IPv6'], 1, 'medium', 'Static NAT is usually a one-to-one design, so each published service commonly consumes one public address.'),
  q('d4-4-2-q4', 'What must be configured correctly for static NAT to work at all?', ['STP priority only', 'NAT inside and NAT outside interface roles', 'Only a default route', 'Only a DHCP scope'], 1, 'easy', 'The router must know which interfaces are inside and outside so it knows where translation applies.'),
  q('d4-4-2-q5', 'Which direction of traffic can static NAT support when designed properly?', ['Only inside-to-outside traffic', 'Only outside-to-inside traffic', 'Both outbound and inbound reachability', 'Neither direction'], 2, 'medium', 'Static NAT supports predictable bidirectional reachability when routing and interface roles are correct.'),
  q('d4-4-2-q6', 'If the static mapping exists but access still fails, what is the best next thought?', ['Check routing and return-path reachability in addition to the translation', 'Delete the translation immediately', 'Assume the DNS server is always wrong', 'Change the VLAN native setting'], 0, 'medium', 'A configured translation alone does not guarantee success; the path and return path still have to work.'),
  q('d4-4-2-q7', 'Which command output is most directly associated with verifying NAT translation state?', ['show ip nat translations', 'show spanning-tree', 'show vlan brief', 'show ntp associations'], 0, 'easy', 'show ip nat translations is a core verification command for NAT state.'),
  q('d4-4-2-q8', 'What happens conceptually when outside traffic is sent to the published public IP of a static NAT server?', ['The router translates it back toward the inside local host', 'The router drops it automatically', 'The router converts it to OSPF', 'The router changes it into a DHCP Offer'], 0, 'medium', 'Inbound traffic to the public mapping can be translated back to the correct inside host in a static NAT design.'),
  q('d4-4-2-q9', 'Which statement best distinguishes static NAT from PAT?', ['Static NAT is one-to-one; PAT shares one public IP across many sessions using ports', 'Static NAT uses UDP only; PAT uses TCP only', 'Static NAT is for IPv6 only; PAT is for IPv4 only', 'There is no difference'], 0, 'medium', 'Static NAT is one-to-one, while PAT multiplexes many sessions through a shared public IP with unique ports.'),
  q('d4-4-2-q10', 'What is the cleanest mental model for static NAT?', ['One inside host, one stable public identity', 'Many inside hosts, one public port table only', 'One routing protocol, one area', 'One DHCP pool, one lease'], 0, 'easy', 'Static NAT is easiest to recognize as a one-to-one mapping with a stable external identity.'),
];

export default staticNatBank;
