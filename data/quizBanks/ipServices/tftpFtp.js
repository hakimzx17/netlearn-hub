import { q } from './helpers.js';

const tftpFtpBank = [
  q('d4-4-11-q1', 'Which statement best describes TFTP?', ['It is a simple lightweight file transfer protocol', 'It is the encrypted replacement for SSH', 'It is a routing protocol', 'It is an NTP hierarchy protocol'], 0, 'easy', 'TFTP is intentionally simple and lightweight compared with richer file-transfer protocols.'),
  q('d4-4-11-q2', 'Which port is commonly associated with the initial TFTP request?', ['21', '22', '53', '69'], 3, 'easy', 'UDP 69 is the well-known initial request port for TFTP.'),
  q('d4-4-11-q3', 'What best distinguishes FTP from TFTP?', ['FTP uses only one UDP port with no control channel', 'FTP uses a separate control channel and data connection model', 'FTP is only for IPv6', 'FTP cannot transfer files from servers'], 1, 'medium', 'FTP is richer and more complex because it uses a separate control channel and data connection behavior.'),
  q('d4-4-11-q4', 'Which TCP port is primarily associated with FTP control?', ['20', '21', '22', '23'], 1, 'easy', 'TCP 21 is associated with FTP control traffic.'),
  q('d4-4-11-q5', 'Which statement about authentication is accurate?', ['FTP commonly expects user authentication, while TFTP is much more minimal', 'TFTP requires stronger user authentication than FTP', 'Neither protocol has any operational differences', 'TFTP is always encrypted by default'], 0, 'medium', 'FTP commonly uses user authentication, whereas TFTP is much more minimal and often treated as unauthenticated.'),
  q('d4-4-11-q6', 'Why are TFTP and FTP often discussed in Cisco-style administration?', ['They appear in file copy and image management workflows', 'They replace the routing table', 'They elect the default gateway', 'They provide QoS marking'], 0, 'easy', 'Both protocols often appear in copy workflows for images, configurations, and related device administration tasks.'),
  q('d4-4-11-q7', 'What is a reasonable security conclusion about classic FTP and TFTP?', ['They are traditional tools but not strong modern secure defaults', 'They are more secure than SSH', 'They replace SNMPv3', 'They should always be exposed publicly'], 0, 'medium', 'Classic FTP and TFTP are traditional administrative tools, but they are not ideal secure defaults for hostile environments.'),
  q('d4-4-11-q8', 'Which statement about TFTP is operationally correct?', ['It is richer than FTP because it has more control features', 'It is simpler than FTP but offers fewer protections and capabilities', 'It is only used with DHCP relay', 'It always authenticates users before transfer'], 1, 'medium', 'TFTP is simpler and lighter than FTP, but that simplicity means fewer capabilities and less inherent protection.'),
  q('d4-4-11-q9', 'What is the best description of a Cisco-style copy workflow?', ['A source-to-destination file movement decision such as copying an image to flash', 'A routing protocol adjacency', 'An STP convergence state', 'An SNMP trap'], 0, 'medium', 'A copy workflow is essentially choosing a source and destination for moving a file such as an image or configuration.'),
  q('d4-4-11-q10', 'Which summary best compares TFTP and FTP?', ['TFTP is lightweight and minimal; FTP is richer with separate control/data behavior', 'TFTP and FTP are identical protocols', 'TFTP is for NAT only and FTP is for DNS only', 'FTP is simpler than TFTP'], 0, 'easy', 'TFTP is lightweight and minimal, while FTP is richer and uses distinct control/data behavior.'),
];

export default tftpFtpBank;
