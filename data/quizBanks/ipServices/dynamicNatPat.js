import { q } from './helpers.js';

const dynamicNatPatBank = [
  q('d4-4-3-q1', 'What does dynamic NAT use to translate inside hosts?', ['A permanent one-to-one mapping only', 'A pool of public addresses allocated as needed', 'A routing table entry', 'A VLAN trunk'], 1, 'easy', 'Dynamic NAT uses a pool of public addresses and allocates translations as sessions require them.'),
  q('d4-4-3-q2', 'What does PAT add beyond dynamic NAT?', ['It removes the need for interface roles', 'It allows many inside hosts to share one public IP by using unique source ports', 'It disables ACL matching', 'It replaces DNS'], 1, 'easy', 'PAT, or NAT overload, scales by distinguishing sessions with Layer 4 port numbers.'),
  q('d4-4-3-q3', 'Which translation model is most common for ordinary client Internet access?', ['Static NAT', 'PAT overload', 'No NAT at all', 'OSPF'], 1, 'easy', 'PAT is the common model for many inside clients sharing one public address for outbound access.'),
  q('d4-4-3-q4', 'Why can plain dynamic NAT without overload run out of usable translations?', ['Because every host shares one address and one port only', 'Because the public address pool can be exhausted', 'Because it does not need routing', 'Because it works only on serial links'], 1, 'medium', 'Dynamic NAT relies on a pool of public addresses, so the pool can be exhausted if demand exceeds supply.'),
  q('d4-4-3-q5', 'What is the purpose of an ACL in many dynamic NAT/PAT configurations?', ['To identify which inside source traffic should be translated', 'To elect the NTP source', 'To define the DNS resolver hierarchy', 'To choose the syslog severity'], 0, 'medium', 'The ACL commonly defines which inside source traffic is eligible for translation.'),
  q('d4-4-3-q6', 'What does the word overload imply in a NAT configuration?', ['Many internal sessions can share one public IP by using ports', 'The router is out of memory', 'The DHCP pool is full', 'The interface is down'], 0, 'easy', 'Overload refers to many inside sessions sharing one public address with unique port translations.'),
  q('d4-4-3-q7', 'Why is a translation table especially important in PAT troubleshooting?', ['It shows how address-plus-port combinations are being mapped', 'It replaces the routing table', 'It stores VLAN membership', 'It shows only NTP peers'], 0, 'medium', 'PAT relies on address-plus-port state, so the translation table is direct evidence of session mapping.'),
  q('d4-4-3-q8', 'Which scenario best matches dynamic NAT rather than PAT?', ['Ten users share one public IP using unique ports', 'A host receives one temporary public address from a defined pool', 'A server has one permanent static public mapping', 'A switch relays DHCP'], 1, 'medium', 'Dynamic NAT means the host is mapped temporarily to one address from a pool, not multiplexed through ports on one address.'),
  q('d4-4-3-q9', 'What remains true for both dynamic NAT and PAT?', ['Routing no longer matters', 'Inside and outside interfaces still have to be marked correctly', 'ACLs are never used', 'The router cannot verify translation state'], 1, 'easy', 'Both translation models still depend on correct inside/outside roles and normal path availability.'),
  q('d4-4-3-q10', 'If new outbound sessions suddenly fail in a dynamic NAT design without overload, what is a strong first suspicion?', ['Pool exhaustion', 'STP loop prevention', 'SSH transport mode', 'Syslog severity mismatch'], 0, 'medium', 'If dynamic NAT cannot allocate more public addresses, new sessions can fail because the pool is exhausted.'),
];

export default dynamicNatPatBank;
