import { q } from './helpers.js';

const natConceptsTerminologyBank = [
  q('d4-4-1-q1', 'What is the primary purpose of NAT in a common enterprise edge design?', ['To elect a default gateway', 'To translate address representation at the network edge', 'To build a routing table', 'To replace DHCP'], 1, 'easy', 'NAT changes address representation at the boundary between networks, typically from private to public addressing.'),
  q('d4-4-1-q2', 'Which term describes the internal host address as it exists on the inside network before translation?', ['Inside global', 'Inside local', 'Outside local', 'Outside global'], 1, 'easy', 'Inside local is the inside host address as it exists on the internal network before translation.'),
  q('d4-4-1-q3', 'Which term describes the public representation of an inside host after translation?', ['Inside global', 'Inside local', 'Outside global', 'Private global'], 0, 'easy', 'Inside global is the translated public representation of the inside host.'),
  q('d4-4-1-q4', 'Why is NAT terminology considered perspective-based?', ['Because the same endpoint can be described differently depending on which side of the translation boundary you are viewing', 'Because NAT uses only IPv6', 'Because routers cannot route without NAT', 'Because every NAT term is vendor-specific'], 0, 'medium', 'NAT terms describe how an address is viewed from the translation boundary, not a completely different endpoint.'),
  q('d4-4-1-q5', 'Which pair of labels identifies the side of the translation boundary rather than the address scope?', ['Local and global', 'Inside and outside', 'Private and public', 'Static and dynamic'], 1, 'medium', 'Inside and outside identify the side of the boundary; local and global describe the address perspective.'),
  q('d4-4-1-q6', 'Which statement best describes routing versus NAT?', ['Routing changes the source IP; NAT chooses the next hop', 'Routing chooses the path; NAT changes address representation', 'Routing and NAT are identical', 'Routing is used only after NAT fails'], 1, 'medium', 'Routing decides where traffic goes, while NAT changes how the addresses appear at the edge.'),
  q('d4-4-1-q7', 'An internal PC 192.168.10.10 is translated to 203.0.113.10. Which address is the inside global?', ['192.168.10.10', '203.0.113.10', 'The outside server address', 'The default gateway IP'], 1, 'easy', 'The inside global is the translated public address representing the inside host externally.'),
  q('d4-4-1-q8', 'What is commonly true about the outside global address in a basic NAT discussion?', ['It is the real address of the outside host as known globally', 'It must always be private', 'It is the translated address of the inside host', 'It is the same thing as the router ID'], 0, 'medium', 'Outside global usually refers to the real address of the outside host as it exists globally.'),
  q('d4-4-1-q9', 'Why is NAT common with RFC 1918 private addressing?', ['Because private addresses are usually not Internet-routable directly', 'Because private addresses cannot be used on LANs', 'Because NAT replaces default routing', 'Because RFC 1918 addresses are only for IPv6'], 0, 'easy', 'Private RFC 1918 addressing normally requires translation before access to public IPv4 destinations.'),
  q('d4-4-1-q10', 'What is the best first step when solving a NAT terminology question?', ['Identify the inside device and the translation boundary first', 'Memorize the outside host immediately', 'Ignore the original inside address', 'Start by checking OSPF metrics'], 0, 'medium', 'NAT terminology is easiest when you identify the inside host and translation boundary first, then label the address perspectives.'),
];

export default natConceptsTerminologyBank;
