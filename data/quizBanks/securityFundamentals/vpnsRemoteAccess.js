import { q } from './helpers.js';

const vpnsRemoteAccessBank = [
  q('d5-5-13-q1', 'What is the main purpose of a remote-access VPN?', ['Securely connect an individual user or device into the network', 'Join two branch networks permanently', 'Replace AAA', 'Control ARP validation'], 0, 'easy', 'A remote-access VPN securely connects an individual user or device into the network.'),
  q('d5-5-13-q2', 'What best distinguishes remote-access VPNs from site-to-site VPNs?', ['Remote-access VPNs are user-to-network, not network-to-network', 'Remote-access VPNs never use encryption', 'Remote-access VPNs are only for routers', 'Remote-access VPNs replace firewalls'], 0, 'easy', 'Remote-access VPNs are user-to-network rather than network-to-network designs.'),
  q('d5-5-13-q3', 'Which two high-level remote-access approaches are commonly compared in CCNA-level discussions?', ['TLS-based and IPsec-based client VPNs', 'ARP-based and DHCP-based tunnels', 'SNMP-based and Syslog-based tunnels', 'VTP-based and STP-based tunnels'], 0, 'medium', 'TLS-based and IPsec-based client VPNs are the common high-level comparison in remote access.'),
  q('d5-5-13-q4', 'What is the best description of AnyConnect in this context?', ['A remote-access client platform that can use different secure tunnel models depending on deployment', 'A fixed-only GRE tunnel', 'A replacement for DHCP Snooping', 'A wireless encryption standard'], 0, 'medium', 'AnyConnect is best understood as a remote-access client platform rather than one fixed tunnel technology.'),
  q('d5-5-13-q5', 'Why does user experience matter in remote-access VPN design?', ['Because the design has to match the user, device, and access needs', 'Because UX eliminates the need for encryption', 'Because only NTP matters to remote users', 'Because all remote-access VPNs are identical'], 0, 'medium', 'Remote-access VPN choices depend on user, device, and access requirements.'),
  q('d5-5-13-q6', 'Which statement best matches a TLS-based VPN approach?', ['It is often associated with secure application-style remote connectivity', 'It is always a site-to-site-only model', 'It replaces all AAA systems', 'It is unrelated to user access'], 0, 'medium', 'TLS-based VPNs are commonly associated with secure application-style remote connectivity.'),
  q('d5-5-13-q7', 'Which statement best matches an IPsec-based remote-access approach?', ['It emphasizes encrypted network-layer style connectivity', 'It cannot protect user traffic', 'It is the same as DHCP relay', 'It is only for Syslog'], 0, 'medium', 'IPsec-based client VPNs emphasize encrypted network-layer style remote connectivity.'),
  q('d5-5-13-q8', 'What is the first classification question to ask in a VPN scenario?', ['Is this user-to-network or network-to-network?', 'What is the switchport duplex?', 'What is the DNS TTL?', 'What is the STP priority?'], 0, 'easy', 'The first step is deciding whether the problem is user-to-network or network-to-network.'),
  q('d5-5-13-q9', 'Why should AnyConnect not be confused with one single tunnel type?', ['Because the client platform can be deployed with different secure remote-access tunnel models', 'Because it is not a VPN tool at all', 'Because it only supports WEP', 'Because it disables IPsec'], 0, 'medium', 'AnyConnect can support different secure remote-access tunnel models depending on deployment.'),
  q('d5-5-13-q10', 'What is the best summary of remote-access VPN reasoning?', ['Choose the model that best matches the user’s access scope, device, and security needs', 'Always use the same tunnel regardless of user needs', 'Ignore the difference from site-to-site', 'Treat remote access as only a physical-security issue'], 0, 'hard', 'Remote-access VPN design should match the user, access scope, device, and security requirements.'),
];

export default vpnsRemoteAccessBank;
