import { q } from './helpers.js';

const ipv6StaticRoutingBank = [
  q('d3-3-10-q1', 'What global router setting must be enabled before IPv6 forwarding works?', ['ipv6 dhcp relay', 'ipv6 unicast-routing', 'ipv6 helper-address', 'ipv6 nat enable'], 1, 'easy', 'ipv6 unicast-routing is required before the router will forward IPv6 traffic.'),
  q('d3-3-10-q2', 'Why does an IPv6 static route using a link-local next hop also need the exit interface?', ['Because link-local addresses are valid only on the local segment and need interface context', 'Because IPv6 does not support next-hop addresses', 'Because link-local addresses are global by definition', 'Because OSPFv3 requires it'], 0, 'medium', 'A link-local address is scoped to one segment, so the router needs the interface to know where that next hop exists.'),
  q('d3-3-10-q3', 'What is the IPv6 default route prefix?', ['::/0', 'fe80::/10', 'ff00::/8', '2000::/3'], 0, 'easy', '::/0 is the IPv6 default route and serves the same catch-all purpose as 0.0.0.0/0 in IPv4.'),
  q('d3-3-10-q4', 'Which command output is the best first check for verifying an IPv6 static route installed?', ['show ipv6 route', 'show vlan brief', 'show interfaces trunk', 'show mac address-table'], 0, 'easy', 'show ipv6 route is the first verification step for installed IPv6 routes.'),
  q('d3-3-10-q5', 'How does IPv6 static-routing logic compare with IPv4 static routing?', ['It uses the same destination-prefix logic but adds special attention to link-local next-hop behavior', 'It ignores destination prefixes completely', 'It can never use default routes', 'It always requires OSPFv3'], 0, 'medium', 'IPv6 static routing follows the same core destination-prefix model as IPv4 while adding interface-aware next-hop considerations.'),
  q('d3-3-10-q6', 'Which statement about a global unicast next hop in an IPv6 static route is accurate?', ['It can be used as forwarding information much like a normal next-hop route', 'It automatically disables recursive logic', 'It replaces the need for a destination prefix', 'It is valid only on loopback interfaces'], 0, 'medium', 'A global unicast next hop behaves like a normal next-hop reference that the router can use to resolve forwarding.'),
  q('d3-3-10-q7', 'What is a likely outcome if ipv6 unicast-routing is not enabled on the router?', ['IPv6 static routes may be configured, but the router will not forward IPv6 traffic normally', 'The router becomes an OSPF DR automatically', 'IPv4 routing stops working', 'The link-local addresses disappear'], 0, 'medium', 'Without ipv6 unicast-routing, the box will not behave like an IPv6 forwarding router.'),
  q('d3-3-10-q8', 'Which validation approach is best after adding an IPv6 static route?', ['Trust the configuration and skip testing', 'Confirm the route installs and then test real IPv6 reachability with tools like ping', 'Clear the LSDB', 'Remove all link-local addresses'], 1, 'easy', 'Routing-table verification plus real reachability testing is the right validation flow.'),
  q('d3-3-10-q9', 'What does an IPv6 static route still need in order to work operationally?', ['Only a hostname', 'A destination prefix and usable forwarding information', 'A VLAN trunk', 'A DR election'], 1, 'easy', 'Just like IPv4, IPv6 static routing requires both the destination prefix and a usable forwarding path.'),
  q('d3-3-10-q10', 'Which scenario most strongly points to incorrect IPv6 next-hop logic?', ['The route uses a link-local next hop without an exit interface', 'The route is a default route', 'The router has a loopback interface', 'The destination is in another subnet'], 0, 'hard', 'A link-local next hop without an interface is ambiguous and is a classic IPv6 static-routing mistake.'),
];

export default ipv6StaticRoutingBank;
