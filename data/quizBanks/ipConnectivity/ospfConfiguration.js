import { q } from './helpers.js';

const ospfConfigurationBank = [
  q('d3-3-7-q1', 'What is the main purpose of a router ID in OSPF?', ['To replace the need for IP addressing', 'To logically identify the router within OSPF operations and troubleshooting output', 'To choose the native VLAN', 'To encrypt LSAs'], 1, 'easy', 'The router ID is the logical identity OSPF uses for the device during adjacency and topology operations.'),
  q('d3-3-7-q2', 'What is the effect of making an OSPF-enabled interface passive?', ['It disables all Layer 3 forwarding on the router', 'It advertises the connected network without sending Hellos on that interface', 'It forces the interface to become DR', 'It removes the network from the routing table'], 1, 'medium', 'A passive interface still advertises the network but does not attempt neighbor formation there.'),
  q('d3-3-7-q3', 'Which command output is most useful for verifying that OSPF neighbors formed?', ['show ip route ospf', 'show ip ospf neighbor', 'show version', 'show arp'], 1, 'easy', 'show ip ospf neighbor is the first direct verification for adjacency state.'),
  q('d3-3-7-q4', 'Which command output best confirms that OSPF-learned routes are actually installed?', ['show controllers', 'show ip route ospf', 'show ip protocols brief only', 'show interface status'], 1, 'easy', 'show ip route ospf confirms that routes learned through OSPF appear in the routing table.'),
  q('d3-3-7-q5', 'Why would an engineer tune OSPF cost?', ['To influence preferred path selection between competing links', 'To disable DR election', 'To convert OSPF into RIP', 'To remove the need for a router ID'], 0, 'medium', 'OSPF cost tuning changes path preference when multiple OSPF paths exist.'),
  q('d3-3-7-q6', 'What is the purpose of default-information originate in OSPF?', ['To clear all default routes', 'To inject a default route into OSPF so downstream routers can use that device as an exit point', 'To advertise only local host routes', 'To force every interface to stay passive'], 1, 'medium', 'default-information originate advertises a default route into the OSPF domain when appropriate.'),
  q('d3-3-7-q7', 'If an expected OSPF route is missing, what is the best first protocol-specific check?', ['Check adjacency formation before blaming SPF calculations', 'Immediately change the router ID', 'Delete the LSDB', 'Change all interfaces to passive'], 0, 'medium', 'If the adjacency never formed, the routing information will not arrive correctly in the first place.'),
  q('d3-3-7-q8', 'What happens on an interface that participates in OSPF and is not passive?', ['It sends Hello packets appropriate to that OSPF-enabled segment', 'It disables all multicast traffic', 'It becomes an HSRP standby interface', 'It stops installing connected routes'], 0, 'easy', 'A non-passive OSPF interface sends Hellos and participates in neighbor formation.'),
  q('d3-3-7-q9', 'Why is mapping interfaces into the correct area important?', ['Because area mismatch can prevent intended neighbor formation and route exchange', 'Because OSPF uses areas only for IP addressing', 'Because area numbers choose VLAN membership', 'Because areas replace default routes'], 0, 'medium', 'Interfaces placed in the wrong area can break adjacency expectations and route distribution behavior.'),
  q('d3-3-7-q10', 'Which statement best captures post-configuration OSPF verification?', ['Configuration is complete once the commands are accepted', 'Verification means confirming neighbors formed and the expected routes actually appear', 'Verification is needed only for serial interfaces', 'Only the running-config matters, not the routing table'], 1, 'easy', 'OSPF configuration is only operationally complete when adjacency and route outcomes match the design.'),
];

export default ospfConfigurationBank;
