import { q } from './helpers.js';

const firstHopRedundancyHsrpBank = [
  q('d3-3-12-q1', 'What problem does HSRP solve for end hosts?', ['It prevents duplicate MAC learning on a switch', 'It keeps a default gateway function available even if one physical router fails', 'It eliminates the need for IP addresses', 'It builds OSPF adjacencies'], 1, 'easy', 'HSRP provides first-hop gateway redundancy so hosts can keep using one logical default gateway during failure events.'),
  q('d3-3-12-q2', 'What do hosts normally use as their configured default gateway in an HSRP design?', ['The active router physical IP only', 'The standby router physical IP only', 'The HSRP virtual IP address', 'The router ID of Area 0'], 2, 'easy', 'Hosts point to the virtual IP, not to a specific physical router interface.'),
  q('d3-3-12-q3', 'Which HSRP role normally forwards traffic for the virtual gateway?', ['Passive', 'Active', 'Listening-only', 'ABR'], 1, 'easy', 'The active router forwards traffic on behalf of the virtual gateway.'),
  q('d3-3-12-q4', 'What is the job of the standby router in HSRP?', ['It sends all traffic while the active router listens', 'It is ready to take over if the active router fails', 'It replaces the need for a virtual MAC', 'It advertises OSPF externals'], 1, 'easy', 'The standby router monitors the active router and is prepared to take over if needed.'),
  q('d3-3-12-q5', 'What does HSRP preemption allow?', ['A higher-priority router to reclaim the active role after recovering', 'Hosts to choose any gateway at random', 'The standby router to disable the virtual IP', 'Every router to forward simultaneously'], 0, 'medium', 'Preemption lets the preferred higher-priority device take back the active role once it returns.'),
  q('d3-3-12-q6', 'How does HSRP priority affect active-router selection?', ['Lower priority always wins', 'Higher priority is preferred for active status', 'Priority matters only for VLANs', 'Priority is ignored if a virtual IP exists'], 1, 'medium', 'HSRP prefers the router with the higher configured priority.'),
  q('d3-3-12-q7', 'Why is interface tracking useful with HSRP?', ['It can reduce a router’s effective priority when an important upstream path fails', 'It disables the virtual IP permanently', 'It creates DR elections', 'It converts HSRP into GLBP'], 0, 'medium', 'Tracking helps HSRP react to important upstream failures, not just total device failure.'),
  q('d3-3-12-q8', 'Which comparison is accurate?', ['VRRP is generally standards-based, while HSRP is Cisco-oriented', 'HSRP is standards-based and VRRP is Cisco proprietary', 'GLBP provides only standby behavior with no load sharing', 'All FHRPs are identical'], 0, 'medium', 'VRRP is the standards-based alternative, while HSRP is Cisco’s first-hop redundancy protocol.'),
  q('d3-3-12-q9', 'What is GLBP best known for compared with HSRP?', ['Removing the need for a virtual IP', 'Adding load sharing along with gateway redundancy', 'Disabling failover behavior', 'Using only IPv6 link-local addresses'], 1, 'medium', 'GLBP combines first-hop redundancy with load-sharing behavior.'),
  q('d3-3-12-q10', 'Why do hosts usually not need reconfiguration during an HSRP failover?', ['Because they keep using the same virtual gateway identity', 'Because HSRP converts every packet into a broadcast', 'Because TTL is reset to 255', 'Because ARP is disabled'], 0, 'easy', 'The logical gateway identity stays the same, so hosts do not need a new default-gateway configuration during failover.'),
];

export default firstHopRedundancyHsrpBank;
