import { q } from './helpers.js';

const routerOperationBank = [
  q('d3-3-1-q1', 'Which field does a router primarily examine when deciding how to forward a packet between networks?', ['Destination MAC address', 'Destination IP address', 'Source port number', 'Frame Check Sequence'], 1, 'easy', 'Routers make Layer 3 forwarding decisions by matching the destination IP address against routing-table prefixes.'),
  q('d3-3-1-q2', 'Why does a router break up broadcast domains?', ['Because each router interface is a Layer 3 boundary', 'Because routers flood Ethernet broadcasts faster than switches', 'Because routers convert every frame into multicast', 'Because routers disable ARP on connected interfaces'], 0, 'easy', 'Each router interface is a separate Layer 3 boundary, so normal Layer 2 broadcasts are not forwarded between interfaces.'),
  q('d3-3-1-q3', 'An interface in show ip interface brief displays up/up. What is the best interpretation?', ['The interface is shut down but has a saved IP address', 'The interface and line protocol are both operational', 'The physical link is down but routing is enabled', 'The port is waiting for a default gateway'], 1, 'easy', 'up/up means both the physical interface and the data-link condition are working.'),
  q('d3-3-1-q4', 'What normally happens to the Layer 2 header when a router forwards a packet to the next hop?', ['It remains unchanged end to end', 'It is removed and replaced for the outgoing link', 'It is converted into an OSPF Hello packet', 'It becomes the routing-table entry'], 1, 'medium', 'A router strips the incoming frame and builds a new outgoing Layer 2 frame for the next segment.'),
  q('d3-3-1-q5', 'A host sends traffic to a remote subnet through a router. Which statement is correct?', ['The destination MAC stays the same across every routed hop', 'The source and destination IP addresses usually stay end to end while the MAC addresses change hop by hop', 'The router replaces the final destination IP with its own interface IP', 'Both the IP and MAC addresses are rewritten on every routed hop'], 1, 'medium', 'Layer 3 addressing normally stays tied to the endpoints, while Layer 2 addressing changes for each local link.'),
  q('d3-3-1-q6', 'Which action occurs as part of normal packet forwarding on a router?', ['The router increments the TTL before sending', 'The router decrements the TTL before sending the packet onward', 'The router converts TCP into UDP', 'The router floods the packet out every active interface'], 1, 'easy', 'Routers decrement the TTL by one on each routed hop before forwarding the packet.'),
  q('d3-3-1-q7', 'An interface shows administratively down. What should you conclude first?', ['The cable is definitely unplugged', 'The interface is disabled in configuration', 'The neighbor router has failed SPF', 'The default route is missing'], 1, 'easy', 'Administratively down indicates the interface has been shut down in configuration.'),
  q('d3-3-1-q8', 'An interface shows up/down. Which explanation best fits?', ['The interface is fully healthy', 'The interface is shut down manually', 'Physical signal exists but the data-link state is not fully operational', 'The router has no IP route to the destination'], 2, 'medium', 'up/down usually means the physical layer is present but the line protocol or data-link condition is incomplete.'),
  q('d3-3-1-q9', 'Why is show protocols useful during router troubleshooting?', ['It shows only OSPF LSAs', 'It helps verify interface status and whether addressing or protocol state matches expectations', 'It clears the ARP cache automatically', 'It rewrites incorrect static routes'], 1, 'medium', 'show protocols is useful because it summarizes interface state and protocol-related details that help validate operational assumptions.'),
  q('d3-3-1-q10', 'Which comparison best contrasts switching and routing?', ['Switches use destination IP prefixes; routers use destination MAC addresses', 'Switches decide local forwarding inside a VLAN, while routers decide how traffic leaves the current network', 'Switches decrement TTL; routers preserve TTL', 'Switches create separate IP subnets by default, while routers do not'], 1, 'hard', 'Switches handle local Layer 2 forwarding inside the current broadcast domain, while routers select Layer 3 paths toward other networks.'),
];

export default routerOperationBank;
