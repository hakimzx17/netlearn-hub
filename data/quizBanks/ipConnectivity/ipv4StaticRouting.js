import { q } from './helpers.js';

const ipv4StaticRoutingBank = [
  q('d3-3-4-q1', 'What best describes a static route?', ['A route learned from Hello packets', 'A route manually configured by the administrator', 'A route created automatically by ARP', 'A route used only for multicast traffic'], 1, 'easy', 'A static route is manually configured rather than learned through a routing protocol.'),
  q('d3-3-4-q2', 'What is the main purpose of a default static route?', ['To match only loopback traffic', 'To provide a catch-all path when no more specific route matches', 'To advertise OSPF neighbors', 'To replace the MAC table'], 1, 'easy', 'A default route is the least specific route and is used when no more specific prefix exists.'),
  q('d3-3-4-q3', 'If a static route points to a next-hop IP address, what extra step does the router usually perform?', ['It runs SPF', 'It performs recursive lookup to reach the next hop', 'It elects a DR', 'It rewrites the destination IP'], 1, 'medium', 'A next-hop static route requires the router to resolve how to reach that next-hop address.'),
  q('d3-3-4-q4', 'Which static-route style explicitly tells the router which interface to use?', ['Connected route', 'Exit-interface route', 'Local route', 'Dynamic route'], 1, 'easy', 'An exit-interface static route specifies the forwarding interface directly.'),
  q('d3-3-4-q5', 'Why are static routes often useful on stub networks or simple edge links?', ['Because the path is predictable and there may be only one valid way out', 'Because OSPF cannot run on Ethernet', 'Because static routes automatically summarize neighbors', 'Because they remove the need for an IP address'], 0, 'medium', 'Static routes fit well when the topology is simple and the intended path is stable and obvious.'),
  q('d3-3-4-q6', 'Which command is most useful for verifying that a static route installed successfully?', ['show version', 'show ip route', 'show interfaces trunk', 'show vlan brief'], 1, 'easy', 'show ip route is the first place to confirm that the configured prefix is actually present in the routing table.'),
  q('d3-3-4-q7', 'What is the normal design purpose of a floating static route with an AD of 200?', ['To be preferred over a connected route', 'To act as a backup path if the primary route fails', 'To become the default route for all traffic', 'To disable recursive lookup'], 1, 'medium', 'A high-AD static route is intended to stay inactive until the preferred primary route disappears.'),
  q('d3-3-4-q8', 'Which statement about static routes is correct?', ['They never need verification because the syntax is enough', 'They can be accepted in configuration even if the forwarding path is unusable', 'They always beat longest prefix match', 'They automatically create neighbor relationships'], 1, 'hard', 'A static route can be configured correctly in syntax but still fail operationally if the next hop or outgoing path is unusable.'),
  q('d3-3-4-q9', 'What happens if no specific route matches a packet and no default route exists?', ['The router forwards the packet anyway using broadcast', 'The router drops the packet because it has no matching path', 'The router converts the route to connected', 'The router automatically creates a floating static route'], 1, 'easy', 'Without a matching route or default route, the router has no forwarding instruction and drops the packet.'),
  q('d3-3-4-q10', 'Which comparison is accurate?', ['A default route is not a static route', 'A default route is simply the least specific route in the table', 'A default route works only on serial links', 'A default route always has AD 0'], 1, 'medium', 'A default route is still a route; it is simply the least specific match used as a catch-all.'),
];

export default ipv4StaticRoutingBank;
